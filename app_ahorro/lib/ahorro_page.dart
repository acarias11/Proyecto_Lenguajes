import 'package:flutter/material.dart';

class AhorroPage extends StatelessWidget {
  const AhorroPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
            floatingActionButton: FloatingActionButton(onPressed: (){}),

    );
  }
}